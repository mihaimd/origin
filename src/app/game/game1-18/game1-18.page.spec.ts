import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game118Page } from './game1-18.page';

describe('Game118Page', () => {
  let component: Game118Page;
  let fixture: ComponentFixture<Game118Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game118Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
