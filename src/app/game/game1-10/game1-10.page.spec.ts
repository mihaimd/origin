import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game110Page } from './game1-10.page';

describe('Game110Page', () => {
  let component: Game110Page;
  let fixture: ComponentFixture<Game110Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game110Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
