import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game127Page } from './game1-27.page';

describe('Game127Page', () => {
  let component: Game127Page;
  let fixture: ComponentFixture<Game127Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game127Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
