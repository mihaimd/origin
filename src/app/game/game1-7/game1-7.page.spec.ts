import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game17Page } from './game1-7.page';

describe('Game17Page', () => {
  let component: Game17Page;
  let fixture: ComponentFixture<Game17Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game17Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
