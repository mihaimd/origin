import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game111Page } from './game1-11.page';

describe('Game111Page', () => {
  let component: Game111Page;
  let fixture: ComponentFixture<Game111Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game111Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
