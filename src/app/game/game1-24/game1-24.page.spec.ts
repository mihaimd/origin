import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game124Page } from './game1-24.page';

describe('Game124Page', () => {
  let component: Game124Page;
  let fixture: ComponentFixture<Game124Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game124Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
