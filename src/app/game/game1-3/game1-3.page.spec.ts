import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game13Page } from './game1-3.page';

describe('Game13Page', () => {
  let component: Game13Page;
  let fixture: ComponentFixture<Game13Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game13Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
