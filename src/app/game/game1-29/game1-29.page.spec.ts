import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game129Page } from './game1-29.page';

describe('Game129Page', () => {
  let component: Game129Page;
  let fixture: ComponentFixture<Game129Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game129Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
