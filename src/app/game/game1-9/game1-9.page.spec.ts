import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game19Page } from './game1-9.page';

describe('Game19Page', () => {
  let component: Game19Page;
  let fixture: ComponentFixture<Game19Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game19Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
