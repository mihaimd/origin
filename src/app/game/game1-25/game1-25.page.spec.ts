import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game125Page } from './game1-25.page';

describe('Game125Page', () => {
  let component: Game125Page;
  let fixture: ComponentFixture<Game125Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game125Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
