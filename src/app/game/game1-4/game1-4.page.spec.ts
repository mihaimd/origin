import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game14Page } from './game1-4.page';

describe('Game14Page', () => {
  let component: Game14Page;
  let fixture: ComponentFixture<Game14Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game14Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
