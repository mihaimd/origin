import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game113Page } from './game1-13.page';

describe('Game113Page', () => {
  let component: Game113Page;
  let fixture: ComponentFixture<Game113Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game113Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
