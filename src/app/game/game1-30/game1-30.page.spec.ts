import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game130Page } from './game1-30.page';

describe('Game130Page', () => {
  let component: Game130Page;
  let fixture: ComponentFixture<Game130Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game130Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
