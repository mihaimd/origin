import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game128Page } from './game1-28.page';

describe('Game128Page', () => {
  let component: Game128Page;
  let fixture: ComponentFixture<Game128Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game128Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
