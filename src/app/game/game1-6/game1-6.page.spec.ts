import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game16Page } from './game1-6.page';

describe('Game16Page', () => {
  let component: Game16Page;
  let fixture: ComponentFixture<Game16Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game16Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
