import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game112Page } from './game1-12.page';

describe('Game112Page', () => {
  let component: Game112Page;
  let fixture: ComponentFixture<Game112Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game112Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
