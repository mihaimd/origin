import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game119Page } from './game1-19.page';

describe('Game119Page', () => {
  let component: Game119Page;
  let fixture: ComponentFixture<Game119Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game119Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
