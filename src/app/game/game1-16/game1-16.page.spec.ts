import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game116Page } from './game1-16.page';

describe('Game116Page', () => {
  let component: Game116Page;
  let fixture: ComponentFixture<Game116Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game116Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
