import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game122Page } from './game1-22.page';

describe('Game122Page', () => {
  let component: Game122Page;
  let fixture: ComponentFixture<Game122Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game122Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
