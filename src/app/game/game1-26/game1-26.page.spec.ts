import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game126Page } from './game1-26.page';

describe('Game126Page', () => {
  let component: Game126Page;
  let fixture: ComponentFixture<Game126Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game126Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
