import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game115Page } from './game1-15.page';

describe('Game115Page', () => {
  let component: Game115Page;
  let fixture: ComponentFixture<Game115Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game115Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
