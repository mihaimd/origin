import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game114Page } from './game1-14.page';

describe('Game114Page', () => {
  let component: Game114Page;
  let fixture: ComponentFixture<Game114Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game114Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
