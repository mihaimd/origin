import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Game117Page } from './game1-17.page';

describe('Game117Page', () => {
  let component: Game117Page;
  let fixture: ComponentFixture<Game117Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Game117Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
